# pulsar_annual_timing_residuals

This repository cintains scripts to analyze pulsar timing files. To run using uv:

```bash
uv venv
. .venv/bin/activate
uv pip install -r requirements.txt
uv run vlbi_frozen_analysis.py
```
